import type { PolicyValuationInput } from "@/lib/actuarial/types";
import { policies } from "@/lib/demo-data";
function buildPremiumSchedule(valuationDate: string, annualPremium: number) { return Array.from({ length: 15*12 },(_,index)=>{ const date=new Date(`${valuationDate}T00:00:00.000Z`); date.setUTCMonth(date.getUTCMonth()+index+1); return {date:date.toISOString().slice(0,10), amount:annualPremium/12}; }); }
export function demoPolicyInput(policyId="POL-001"): PolicyValuationInput { const policy=policies.find(item=>item.id===policyId)??policies[0]; const valuationDate="2026-04-24"; const birthYear=2026-policy.age; return { policyId:policy.id, valuationDate, settlementType:"STANDARD_LIFE_SETTLEMENT", deathBenefit:policy.faceValue, acquisitionCost:policy.purchasePrice, insured:{anonymizedInsuredId:policy.insured,birthDate:`${birthYear}-01-01`,sex:policy.id.endsWith("2")||policy.id.endsWith("4")||policy.id.endsWith("5")?"F":"M",smokerStatus:"UNKNOWN"}, mortality:{tableId:"DEMO_VBT_2015",mortalityMultiplier:Math.max(0.6,1+(-policy.leDeviation/10)),fractionalAgeAssumption:"CONSTANT_FORCE"}, premiumSchedule:buildPremiumSchedule(valuationDate,policy.annualPremium), discountRate:0.12, targetIrr:0.15, terminalAge:120, expenseLoad:policy.purchasePrice*0.015, reserveRequirement:policy.annualPremium*2, ledgerType:"ECONOMIC" }; }
export const demoPortfolioInputs = () => policies.slice(0,10).map(policy=>demoPolicyInput(policy.id));
export const demoAeInput = { portfolioId:"DEMO-PORTFOLIO-001", analysisStartDate:"2025-01-01", analysisEndDate:"2025-12-31", methodology:"MORTALITY_DISTRIBUTION" as const, applyIbnr:true, cohorts:[ {cohortKey:"2022 vintage",actualDeaths:6,expectedDeaths:5.1,exposureDays:2410,ibnrFactor:1.04}, {cohortKey:"2023 vintage",actualDeaths:3,expectedDeaths:2.9,exposureDays:1980,ibnrFactor:1.05}, {cohortKey:"2024 vintage",actualDeaths:3,expectedDeaths:2.8,exposureDays:1552,ibnrFactor:1.03} ] };
